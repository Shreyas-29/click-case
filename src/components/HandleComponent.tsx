import React from 'react'

const HandleComponent = () => {
    return (
        <div className="w-5 h-5 rounded-full shadow border bg-background border-border transition hover:bg-primary"></div>
    );
};

export default HandleComponent
